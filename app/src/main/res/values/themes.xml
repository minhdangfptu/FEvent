<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FEvent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/red_500</item>
        <item name="colorPrimaryVariant">@color/primary_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary color -->
        <item name="colorSecondary">@color/blue_500</item>
        <item name="colorSecondaryVariant">@color/primary_300</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@color/gray_100</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/gray_700</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/gray_700</item>
        <item name="android:textColorSecondary">@color/gray_600</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/red_500</item>
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Button style (Material) -->
        <item name="buttonStyle">@style/Widget.MaterialComponents.Button</item>

        <!-- Custom Font (if any) -->

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FEvent" parent="Base.Theme.FEvent" />

</resources>